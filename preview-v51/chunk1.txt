    .quote-card p{font-family:var(--mono);text-transform:uppercase;letter-spacing:.16em;font-size:11px;line-height:1.8;max-width:20ch;margin:0;color:rgba(241,236,226,.82)}
    .quote-card .dash{width:32px;height:1px;background:var(--coral)}
    .tile-bw img{filter:grayscale(1) contrast(1.08) brightness(.72)}

    /* EDITORIAL INTRO */
    .philosophy{background:var(--cream);color:var(--ink);padding:110px var(--gutter)}
    .philosophy-grid{max-width:var(--max);margin:auto;display:grid;grid-template-columns:.72fr 1.28fr;gap:clamp(50px,8vw,150px);align-items:start}
    .philosophy h2{font-family:var(--serif);font-weight:300;font-size:clamp(52px,6vw,94px);line-height:.92;letter-spacing:-.045em;margin:0;max-width:8.5ch}
    .philosophy h2 em{color:var(--coral);font-weight:300}
    .philosophy-copy{padding-top:8px}
    .philosophy-copy>p{font-size:clamp(19px,2vw,27px);line-height:1.42;max-width:32ch;margin:0 0 50px}
    .pillars{border-top:1px solid var(--line-light)}
    .pillar{display:grid;grid-template-columns:74px 1fr 1fr;gap:24px;padding:25px 0;border-bottom:1px solid var(--line-light);align-items:start}
    .pillar .num{font-family:var(--mono);font-size:10px;letter-spacing:.12em;color:#6a655e}
    .pillar h3{margin:0;font-size:17px;font-weight:600;text-transform:uppercase;letter-spacing:.06em}
    .pillar p{margin:0;color:#6f685f;font-size:14px;max-width:38ch}

    /* SERVICES */
    .services{background:var(--ink);padding:104px var(--gutter)}
    .section-head{max-width:var(--max);margin:0 auto 58px;display:grid;grid-template-columns:.75fr 1.25fr;gap:40px;align-items:end}
    .section-head h2{font-family:var(--serif);font-weight:300;font-size:clamp(48px,5.6vw,88px);line-height:.93;letter-spacing:-.04em;margin:0;max-width:9ch}
    .section-head p{margin:0;color:rgba(241,236,226,.64);max-width:48ch;justify-self:end}
    .service-grid{max-width:var(--max);margin:auto;display:grid;grid-template-columns:repeat(3,1fr);border-top:1px solid var(--line-dark)}
    .service{padding:34px 30px 42px;border-right:1px solid var(--line-dark);min-height:310px;display:flex;flex-direction:column}
    .service:last-child{border-right:0}
    .service .service-num{font-family:var(--mono);font-size:10px;color:var(--coral);letter-spacing:.15em}
    .service h3{font-family:var(--serif);font-weight:300;font-size:42px;margin:auto 0 14px}
    .service p{margin:0;color:rgba(241,236,226,.62);font-size:14px;max-width:34ch}

    /* EVENTS */
    .events{background:var(--petrol);padding:104px var(--gutter)}
    .event-list{max-width:var(--max);margin:auto;border-top:1px solid var(--line-dark)}
    .event-row{display:grid;grid-template-columns:70px 1.2fr 1fr 26px;gap:24px;align-items:center;padding:26px 0;border-bottom:1px solid var(--line-dark);text-decoration:none;transition:.2s ease}
    .event-row:hover{padding-left:10px;color:var(--coral)}
    .event-row .n{font-family:var(--mono);font-size:10px;color:rgba(241,236,226,.42)}
    .event-row strong{font-family:var(--serif);font-weight:300;font-size:clamp(28px,3.2vw,48px)}
    .event-row span{color:rgba(241,236,226,.56);font-size:13px}

    /* PROOF */
    .proof{background:var(--ink);padding:8px var(--gutter) 104px}
    .proof-grid{max-width:var(--max);margin:auto;display:grid;grid-template-columns:1.12fr .88fr;gap:8px}
    .proof-photo{min-height:610px;position:relative;overflow:hidden}
    .proof-photo img{width:100%;height:100%;object-fit:cover;object-position:center;filter:saturate(.8) contrast(1.04) brightness(.75)}
    .proof-photo::after{content:"";position:absolute;inset:0;background:linear-gradient(to top,rgba(17,19,18,.78),transparent 50%)}
    .proof-label{position:absolute;z-index:2;left:30px;right:30px;bottom:28px;display:flex;justify-content:space-between;align-items:end}
    .proof-label strong{font-family:var(--serif);font-size:42px;font-weight:300}
    .proof-panel{background:var(--cream);color:var(--ink);padding:clamp(34px,5vw,66px);display:flex;flex-direction:column;justify-content:space-between}
    .proof-panel h2{font-family:var(--serif);font-weight:300;font-size:clamp(48px,5vw,76px);line-height:.93;margin:18px 0 30px;max-width:7ch}
    .proof-panel>p{color:#635d55;max-width:38ch}
    .venue-list{margin-top:48px;border-top:1px solid var(--line-light)}
    .venue{display:flex;justify-content:space-between;gap:20px;padding:15px 0;border-bottom:1px solid var(--line-light);font-size:13px}
    .venue span:last-child{font-family:var(--mono);font-size:9px;letter-spacing:.11em;text-transform:uppercase;color:#777066}

    /* PROCESS + FAQ */
    .process{background:var(--cream);color:var(--ink);padding:104px var(--gutter)}
    .process-grid{max-width:var(--max);margin:auto;display:grid;grid-template-columns:.8fr 1.2fr;gap:80px}
    .process h2{font-family:var(--serif);font-size:clamp(50px,5vw,82px);font-weight:300;line-height:.92;margin:0}
    .steps{border-top:1px solid var(--line-light)}
    .step{display:grid;grid-template-columns:62px 1fr;padding:23px 0;border-bottom:1px solid var(--line-light);gap:20px}
    .step .num{font-family:var(--mono);font-size:10px;color:#746c62}
    .step h3{margin:0 0 5px;font-size:15px;text-transform:uppercase;letter-spacing:.06em}
    .step p{margin:0;color:#6e665e;font-size:14px}

    .faq{background:var(--ink);padding:104px var(--gutter)}
    .faq-wrap{max-width:1100px;margin:auto}
    .faq h2{font-family:var(--serif);font-size:clamp(50px,5.4vw,86px);font-weight:300;line-height:.94;margin:0 0 54px}
    details{border-top:1px solid var(--line-dark)}
    details:last-child{border-bottom:1px solid var(--line-dark)}
    summary{list-style:none;cursor:pointer;padding:22px 38px 22px 0;position:relative;font-size:17px}
    summary::-webkit-details-marker{display:none}
    summary::after{content:"+";position:absolute;right:0;color:var(--coral);font-family:var(--mono)}
    details[open] summary::after{content:"–"}
    details p{color:rgba(241,236,226,.62);max-width:68ch;margin:0;padding:0 0 24px}

    /* CONTACT */
    .contact{background:var(--coral);color:var(--ink);padding:98px var(--gutter)}
    .contact-inner{max-width:var(--max);margin:auto;display:grid;grid-template-columns:1.25fr .75fr;gap:60px;align-items:end}
    .contact h2{font-family:var(--serif);font-weight:300;font-size:clamp(58px,7vw,112px);line-height:.85;letter-spacing:-.05em;margin:12px 0 0;max-width:8ch}
    .contact-actions{display:flex;flex-direction:column;align-items:stretch;gap:10px}
    .contact .btn-primary{background:var(--ink);color:var(--cream)}
    .contact .btn-secondary{border-color:rgba(17,19,18,.5);color:var(--ink)}
    .contact-info{font-family:var(--mono);font-size:10px;letter-spacing:.08em;margin-top:14px}

    footer{background:var(--ink);padding:28px var(--gutter);border-top:1px solid var(--line-dark)}
    .footer-inner{max-width:var(--max);margin:auto;display:flex;align-items:center;justify-content:space-between;gap:20px;color:rgba(241,236,226,.5);font-family:var(--mono);font-size:9px;letter-spacing:.14em;text-transform:uppercase}

    .mobile-bar{display:none}
    .reveal{opacity:0;transform:translateY(18px);transition:opacity .75s ease,transform .75s ease}
    .reveal.in{opacity:1;transform:none}

    @media(max-width:980px){
      .nav-links a:not(.nav-cta){display:none}
      .hero{padding-top:96px}
      .hero-layout{grid-template-columns:1fr;min-height:auto}
      .hero-main{min-height:760px}
      .hero-copy{width:58%}
      .hero-side{grid-template-columns:1fr 1fr;grid-template-rows:1fr}
      .hero-statement,.hero-side-photo{min-height:310px}
      .mosaic-grid{grid-template-columns:1fr 1fr}.mosaic-grid>.tile:last-child{grid-column:1/-1;min-height:380px}
      .philosophy-grid,.section-head,.process-grid,.contact-inner{grid-template-columns:1fr;gap:42px}
      .section-head p{justify-self:start}
      .service-grid{grid-template-columns:1fr}.service{border-right:0;border-bottom:1px solid var(--line-dark);min-height:230px}.service:last-child{border-bottom:0}
      .proof-grid{grid-template-columns:1fr}.proof-photo{min-height:520px}
    }

    @media(max-width:660px){
      .nav{height:70px}.brand{font-size:12px}.nav-cta{padding:8px 10px!important;font-size:10px!important}
      .hero{padding:82px 12px 12px;min-height:auto}
      .hero-layout{display:flex;flex-direction:column;gap:8px}
      .hero-main{display:flex;flex-direction:column;min-height:0}
      .hero-main::after{display:none}
      .hero-photos{position:relative;height:56svh;min-height:390px;order:1}
      .hero-photo{background-position:center 28%!important;filter:saturate(.82) contrast(1.03) brightness(.78)}
      .hero-copy{order:2;width:100%;padding:30px 22px 74px;background:var(--ink)}
      .hero h1{font-size:clamp(49px,15vw,72px);max-width:9ch}
      .hero-note{left:22px;bottom:24px}
      .hero-side{display:grid;grid-template-columns:1fr;gap:8px}.hero-statement{min-height:270px}.hero-side-photo{min-height:420px}
      .hero-strip{display:none}
      .mosaic{padding-inline:12px}.mosaic-grid{display:flex;flex-direction:column}.tile{min-height:400px}.tile-middle{display:flex;flex-direction:column}.quote-card{min-height:230px}.tile-middle .tile{min-height:420px}
